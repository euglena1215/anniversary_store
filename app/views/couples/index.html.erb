<p id="notice"><%= notice %></p>

<h1>Couples</h1>

<table>
  <thead>
    <tr>
      <th>User1</th>
      <th>User2</th>
      <th>Ended at</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @couples.each do |couple| %>
      <tr>
        <td><%= couple.user1_id %></td>
        <td><%= couple.user2_id %></td>
        <td><%= couple.ended_at %></td>
        <td><%= link_to 'Show', couple %></td>
        <td><%= link_to 'Edit', edit_couple_path(couple) %></td>
        <td><%= link_to 'Destroy', couple, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Couple', new_couple_path %>
